<nav class="navbar navbar-expand-lg" style="background-color: #6269a1;width: 100%;">
              <!--<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span></button> -->
      
      <div class="navbar-collapse" id="navbarTogglerDemo01">
            <a class="navbar-brand" ><%= image_tag 'logo.png', style:'height:50px;width:150px;' %></a>
            
            <!--<a class="user-txt text-dark">
              <%# if current_user %>    
                      <%# if current_user.admin? %>
                        <h4>Hi! <%# current_user.username.capitalize %> (Admin)</h4>
                      <%# else %>
                        <h4>Hi! <%# current_user.username.capitalize %> </h4>
                      <%# end %>
              <%# end %></a> -->
                      <!-- <div class="salution-header">
                          <%# if Time.zone.now < Time.zone.now.middle_of_day %>
                              <h4 class="usr-salut-head">Good Morning! <%# current_user.username %> (Admin)</h4>
                          <%# elsif Time.zone.now > Time.zone.now.middle_of_day && Time.zone.now < (Time.zone.now.middle_of_day+4.hours) %>
                              <h4 class="usr-salut-head">Good Afternoon! <%# current_user.username %> (Admin)</h4>
                          <%# else %>
                              <h4 class="usr-salut-head">Hi! <%# current_user.username %> (Admin)</h4>
                          <%# end %>
                      </div>
                    
                      
                      <%# if Time.zone.now < Time.zone.now.middle_of_day %>
                          <h4 class="usr-salut-head">Good Morning! <%# current_user.username %></h4>
                      <%# elsif Time.zone.now > Time.zone.now.middle_of_day && Time.zone.now < (Time.zone.now.middle_of_day+4.hours) %>
                          <h4 class="usr-salut-head">Good Afternoon! <%# current_user.username %></h4>
                      <%# else %>
                          <h4 class="usr-salut-head">Good Evening! <%# current_user.username %></h4> 
                      <%# end %> -->
            <% if current_user %>
                <div class="d-grid col-6 mx-auto">
                                <ul class="navbar-nav nav-fill justify-content-right">
                                      <li class="nav-item">
                                        <a><%= link_to "Home", root_path, class: 'nav-link active fw-bolder'%></a>
                                      </li>
                                      <li class="nav-item">
                                        <a><%= link_to "Students", students_path, class: 'nav-link active fw-bolder'%></a>
                                      </li>                                  
                                </ul>
                </div>
            <% else %>
                <div class="d-grid col-6 mx-auto text-center">
                    <h4 class="nav-header-txt">Fee Management Service</h4>
                </div>
            <% end %>

            <div class="d-grid ml-auto">

              <% if current_user %>
                  <div class="btn-group">

                    <div><%= link_to "Dashboard",admin_dashboard_index_path, class: "btn btn btn-outline-info rounded", style: 'margin-right: 1.5em;' if current_user.admin? %></div>

                    <div class="dropdown" style="margin-right: 1.5em;">                          
                            <button class="btn btn-outline-info dropdown-toggle rounded" type="button" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                              Menu
                            </button>
                            <div class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDropdown">
                              <% if current_user.admin? %>
                                  <a><%= link_to 'TC Student List', removed_students_path , class:'dropdown-item' %></a>
                                  <a><%= link_to "Cheque Data", cheque_invoices_path, class: 'dropdown-item' %></a>
                              <% end %>
                                  <a><%= link_to 'Pending Fee List', pending_fee_students_path, class:'dropdown-item' %></a>
                                  <a><%= link_to "Missing Data", missing_students_path, class: 'dropdown-item'%></a>
                            </div>
                    </div>

                    <div class="dropdown">
                      <button class="btn btn-outline-info dropdown-toggle rounded" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                        <% if current_user.admin? %>
                          <%= current_user.username.titleize %> (Admin)
                        <% else %>
                          <%= current_user.username.titleize %>
                        <% end %>
                      </button>
                        <ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end text-center" aria-labelledby="dropdownMenuButton1">
                          <li><%= button_to "Log Out", destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
                        </ul>
                    </div>                    
                </div>
              <% end %>

              <% if !current_user %>
              <div class="btn-group">
                <div><%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-outline-info rounded', style: 'margin-right:10px' %></div>
                <div><%= link_to "Log In", new_user_session_path, class: 'btn btn-outline-info rounded' %></div>
              </div>
              <% end %>
            </div> 
            
      </div>
    </nav>